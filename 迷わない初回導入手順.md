# Ein Bridge Publisher — 迷わない初回導入手順

## 表示の意味

- **必須**：この方式では記載どおりにする
- **推奨**：変更できるが、初回は記載どおりにする
- **変更可**：好みで変えられる。初回は変更不要

## 1. GitHubで新しいRepositoryを作る

| 画面項目 | 入力値 | 区分 |
|---|---|---|
| Owner | 自分のGitHubアカウント | 推奨 |
| Repository name | `ein-bridge-publisher` | 変更可 |
| Description | `Ein Bridge Publisher deployment source` | 変更可 |
| Visibility | `Public` | 必須 |
| Add a README file | チェックしない | 推奨 |
| Add .gitignore | `None` | 推奨 |
| Choose a license | `None` | 推奨 |

PublicはDeploy to Cloudflare方式で必要です。GitHubにはコードだけを置き、秘密情報や公開資料は置きません。

## 2. ファイルをアップロードする

1. 作ったRepositoryを開く
2. `Add file` → `Upload files`
3. ZIPを「すべて展開」
4. 外側のフォルダではなく、その**中身を全部**アップロード
5. Commit message：`Initial Ein Bridge Publisher setup`
6. `Commit directly to the main branch`

アップロード後、Repositoryの最初の画面に `package.json`、`wrangler.jsonc`、`src` が直接見えることを確認します。

## 3. GitHub URLをChatGPTへ送る

形式：

`https://github.com/あなたのGitHub名/ein-bridge-publisher`

ここから先は、Cloudflareの実際の画面に合わせて1画面ずつ進めます。

## 後で使う採用値

| 項目 | 採用値 | 区分 |
|---|---|---|
| Worker name | `ein-bridge-publisher` | 変更可・初回固定 |
| Cloudflare側Repository name | `ein-bridge-publisher-cloudflare` | 変更可 |
| 管理者メール | `koube201@gmail.com` | 固定 |
| 管理画面 | `publisher.ein-8.com` | 推奨固定 |
| 公開資料 | `docs.ein-8.com` | 推奨固定 |
| Google Cloud project | `Ein Bridge Publisher` | 変更可 |
| OAuth client name | `Ein Bridge Publisher Web` | 変更可 |
| Application type | `Web application` | 必須 |
| Authorized JavaScript origin | `https://publisher.ein-8.com` | 必須 |
| SESSION_SECRET | 後で自動生成 | 秘密・GitHub禁止 |

## 別方式について

GitHub Desktop、npm、BAT、R2、Cloudflare Accessなど別方式も技術的には可能です。ただし、今回は手順と検証を安定させるため、次に固定します。

`GitHub → Deploy to Cloudflare → Googleログイン → 独自ドメイン`
