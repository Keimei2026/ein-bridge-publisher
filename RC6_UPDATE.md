# RC6 update

公開ページの表示不具合を修正しました。

- `/p/<slug>` から版ID付きURLへ転送しないよう変更
- 公開URLを常に `/p/<slug>` のまま維持
- 公開HTMLを同一オリジンのラッパー内で表示できるようCSPを修正
- RC5以前の版ID付きURLは安定URLへ戻す

既存データの再アップロードは不要です。GitHubへ反映後、Cloudflareの自動デプロイが完了すれば、既に公開した資料も表示されます。
